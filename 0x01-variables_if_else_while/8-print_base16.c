#include <stdio.h>
/**
 * main - The main function to print numbers in hexadecimal
 * Return: 0(Success)
 */
int main(void)
{
	int a;

	for (a = 0; a < 10; a++)
	{
		printf("%x", a);
	}
	putchar(a);
	a++;
	return (0);
}
